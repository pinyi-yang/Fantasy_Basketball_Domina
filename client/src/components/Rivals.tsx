import React from 'react';
import {RouteComponentProps} from 'react-router-dom'

const Rivals: React.FC = () => {
  return (
    <div className="rivals">
      here will render the current score comparison of all user rivals
    </div>
  )
}

export default Rivals;